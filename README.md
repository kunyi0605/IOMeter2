# IOMeter
# IOMeter
¡Kor create a new repository on the command line
echo "# IOMeter" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/kunyi0605/IOMeter.git
git push -u origin main

¡Kor push an existing repository from the command line
git remote add origin https://github.com/kunyi0605/IOMeter.git
git branch -M main
git push -u origin main

¡Kor import code from another repository
You can initialize this repository with code from a Subversion, Mercurial, or TFS project.# IOMeter
# IOMeter
